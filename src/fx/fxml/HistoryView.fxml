<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="fx.controller.HistoryController"
      spacing="0"
      styleClass="content-area">
    
    <!-- Header Section -->
    <StackPane fx:id="headerPane" minHeight="90" maxHeight="90">
        <HBox alignment="CENTER_LEFT" spacing="16">
            <padding><Insets top="20" right="28" bottom="20" left="28"/></padding>
            
            <Label text="🕐" style="-fx-font-size: 32px;"/>
            
            <VBox spacing="4">
                <Label text="Search History" style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: white;"/>
                <Label fx:id="countLabel" text="0 recent searches" 
                       style="-fx-font-size: 13px; -fx-text-fill: rgba(255,255,255,0.8);"/>
            </VBox>
            
            <Region HBox.hgrow="ALWAYS"/>
            
            <Button text="Clear All" styleClass="button-header" onAction="#handleClearAll"
                    prefWidth="90"/>
            
            <Button text="🔙 Back" styleClass="button-header" onAction="#handleBack"
                    prefWidth="90"/>
        </HBox>
    </StackPane>
    
    <!-- Content -->
    <VBox VBox.vgrow="ALWAYS" spacing="0">
        <padding><Insets top="20" right="0" bottom="0" left="0"/></padding>
        
        <!-- History List -->
        <ListView fx:id="historyList" VBox.vgrow="ALWAYS" styleClass="list-view"/>
        
        <!-- Empty State -->
        <VBox fx:id="emptyState" alignment="CENTER" spacing="16" visible="false" managed="false">
            <Label text="🕐" style="-fx-font-size: 48px; -fx-opacity: 0.4;"/>
            <Label text="No search history" styleClass="text-heading" style="-fx-opacity: 0.6;"/>
            <Label text="Your recent searches will appear here" styleClass="text-caption"/>
        </VBox>
    </VBox>
    
    <!-- Footer Actions -->
    <HBox spacing="12" alignment="CENTER">
        <padding><Insets top="16" right="0" bottom="0" left="0"/></padding>
        
        <Button text="Search Again" styleClass="button-primary" onAction="#handleSearchAgain" prefWidth="120"/>
        <Button text="Add to Favourites" styleClass="button-outline" onAction="#handleAddToFavorites" prefWidth="160"/>
    </HBox>
</VBox>
